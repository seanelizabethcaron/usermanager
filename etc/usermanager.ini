[database]

user = 
passwd =
db = usermanager
host = 

[logging]

audit = True
auditlog = /tmp/usermanager_audit_log

debug = True
debuglog = /tmp/usermanager_debug_log

[email]

emailreceipt = True
emailreceiptdir = /tmp

disabled_tpl = /opt/usermanager/etc/account_disabled.tpl
pending_tpl = /opt/usermanager/etc/approver_new_pending_request.tpl
complete_dce_tpl = /opt/usermanager/etc/complete_dce101.tpl
complete_itse_tpl = /opt/usermanager/etc/complete_itse106.tpl
complete_both_tpl = /opt/usermanager/etc/complete_dce101_and_itse106.tpl
newaccount_tpl = /opt/usermanager/etc/new_account_information.tpl
newaccount_samba_tpl = /opt/usermanager/etc/new_account_information_samba.tpl
approved_tpl = /opt/usermanager/etc/requestor_account_approved.tpl
denied_tpl = /opt/usermanager/etc/requestor_account_denied.tpl
confirm_tpl = /opt/usermanager/etc/requestor_confirm_request.tpl

[privateldap]

host = 
user_basedn = ou=People,dc=csg,dc=sph,dc=umich,dc=edu
group_basedn = ou=Groups,dc=csg,dc=sph,dc=umich,dc=edu
adminuser = cn=admin,dc=csg,dc=sph,dc=umich,dc=edu
adminpass = 

[campusldap]

host = 
basedn = ou=People,dc=umich,dc=edu

[approvers]

approvers = scaron

[administrators]

administrators = scaron,sdushett

[mcommunity]

clientid = 
clientsecret = 

[maislinc]

mais_dbhost = 
mais_dbuser = 
mais_passwd = 
mais_dbport = 
mais_db = 

[homeagent]

default_permissions = 755
